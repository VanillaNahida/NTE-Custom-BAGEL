#pragma once

// Centralized version number — all components read from here.
// Update this single file when releasing a new version.

#define APP_VERSION   "1.0.6"    // Narrow string (for char* contexts)
#define APP_VERSION_W L"1.0.6"   // Wide string  (for wchar_t* contexts)
